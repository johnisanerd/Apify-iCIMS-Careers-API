[project]
name = "apify-icims-careers-api-example"
version = "0.1.0"
description = "Python example for the iCIMS API on Apify: pull live job postings from iCIMS career sites."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3,<4",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
